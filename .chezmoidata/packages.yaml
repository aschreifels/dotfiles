packages:
  darwin:
    taps:
    brews:
      # Shell & prompt
      - starship
      - zoxide
      - fzf
      - direnv

      # Modern CLI
      - bat
      - eza
      - fd
      - ripgrep
      - jq
      - yq

      # Charm CLI stack (pretty scripts & TUIs — see CONVENTIONS.md → Tooling)
      - gum        # interaction: choose/filter/confirm/spin/style
      - glow       # markdown rendering in the terminal

      # Git
      - git-delta
      - lazygit
      - chezmoi

      # Editor
      - helix

      # Runtimes
      - asdf

      # Containers & cloud
      - kubernetes-cli
      - k9s

      # LSP servers
      - vtsls
      - gopls
      - yaml-language-server

    casks:
      - font-victor-mono-nerd-font
      - ghostty

    npm:
      - dockerfile-language-server-nodejs
